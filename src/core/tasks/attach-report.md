# Attach Report

**Purpose:** Attach a test report or other file to a work item on the project management platform.

**Parameters:** work_item_id (required), file_path (optional), url (optional), filename (required), content_type (optional)

## Execution
1. READ the platform-specific task at {project-root}/_aria/platform/tasks/attach-report.md
2. FOLLOW its instructions with the parameters above
