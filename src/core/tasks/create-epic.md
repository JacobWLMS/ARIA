# Create Epic

**Purpose:** Create a new epic (project grouping) on the project management platform.

**Parameters:** name (required), description (required), team_id (required), state (optional, default "planned")

## Execution
1. READ the platform-specific task at {project-root}/_aria/platform/tasks/create-epic.md
2. FOLLOW its instructions with the parameters above
3. Return the created epic ID for key map storage
