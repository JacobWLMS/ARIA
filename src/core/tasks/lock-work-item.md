# Lock/Unlock Work Item

**Purpose:** Implement agent locking on a work item. Prevents multiple agents from working on the same item simultaneously.

**Parameters:** work_item_id (required), action ("lock" or "unlock"), agent_name (optional)

## Execution
1. READ the platform-specific task at {project-root}/_aria/platform/tasks/lock-work-item.md
2. FOLLOW its instructions with the parameters above
