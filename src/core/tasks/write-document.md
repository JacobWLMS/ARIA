# Write Document

**Purpose:** Create or update a document on the project management platform.

**Parameters:** title, body_content, project_id (optional), key_map_id (optional), icon (optional)

## Execution
1. READ the platform-specific task at {project-root}/_aria/platform/tasks/write-document.md
2. FOLLOW its instructions with the parameters above
3. Store the created/updated document ID in the key map if key_map_id is provided
